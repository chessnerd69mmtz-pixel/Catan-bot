import fs from 'node:fs';
import assert from 'node:assert/strict';
const src=fs.readFileSync(new URL('../src/main.jsx', import.meta.url),'utf8');
assert.match(src,/function nearPerfectBotPlan\(/,'near-perfect planner is missing');
assert.match(src,/function roadActionPotential\(/,'road objective evaluator is missing');
assert.match(src,/function bestRoadBuildingPair\(/,'Road Building planner is missing');
assert.match(src,/strategicPlan.*settlement/,'settlement strategic-plan wiring is missing');
assert.match(src,/botRoadsThisTurn/,'per-turn road state is missing');
assert.match(src,/handPressure/,'hand-pressure scoring is missing');
assert.ok(/checkWin\(localPlayers\)|winReveal/.test(src),'immediate-win guard is missing');
console.log('NEAR-PERFECT BOT REGRESSION TEST PASSED');